{
  "name": "globaltimeviewer",
  "version": "3.12.2",
  "description": "Global Time Viewer는 여러 시간대를 빠르게 비교하고, 시간 조정/기간 계산/복사/이미지 저장까지 처리할 수 있는 브라우저 기반 도구입니다.",
  "main": "main.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "sync:source-loader": "node ./scripts/sync-source-script-loader.mjs",
    "build": "node ./scripts/build-extension.mjs",
    "build:strict": "node ./scripts/build-extension.mjs --enforce-version-consistency",
    "build:vite": "vite build",
    "ci:gate": "npm run lint && npm run test:coverage && npm run build:strict"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^9.22.0",
    "terser": "^5.46.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.7",
    "yazl": "^3.3.1"
  },
  "dependencies": {
    "luxon": "^3.7.2"
  }
}
