# 변경 이력 (Changelog)

## [3.12.1] - 2026-03-31
### 변경됨
- 고정 시간 탭의 실시간 표시 기능 사용 시 발생하는 UI 깜빡임 및 리프레시 현상을 수정했습니다.
- 고정 시간 탭의 모든 시간 입력창 너비를 85px로 조정하고 텍스트 중앙 정렬을 적용했습니다.
- 실시간 표시(Live Now) 항목의 폰트 굵기를 일반(normal)으로 조정하여 일관성을 높였습니다.
- 타임라인에 고정 시간 모드 전용 '실시간 표시' 인디케이터가 움직이도록 기능을 추가했습니다.
- 내부 브릿지 및 파사드 계층의 매개변수(`isTick`) 전달 누락 문제를 해결했습니다.
- 버전 3.12.1에 맞춰 모든 단위 테스트(863개)와 기대값(Version, Width)을 갱신했습니다.

## [3.11.9] - 2026-03-29
### 변경됨
- 버전 메타데이터를 `3.11.9`로 상향했습니다. (`package.json`, `package-lock.json`, `manifest.json`, `js/modules/app-config.js`, `README.md`)
- 버전 노출 테스트 기대값을 `3.11.9`로 갱신했습니다. (`tests/main-runtime-host-utils-module.test.mjs`)

## [3.11.8] - 2026-03-28
### 변경됨
- 버전 메타데이터를 `3.11.8`로 상향했습니다. (`package.json`, `package-lock.json`, `manifest.json`, `js/modules/app-config.js`, `README.md`)

## [3.11.7] - 2026-03-27
### 변경됨
- 버전 메타데이터를 `3.11.7`로 상향했습니다. (`package.json`, `package-lock.json`, `manifest.json`, `js/modules/app-config.js`)
- `data-transfer.js`의 그룹/보조그룹/전체 설정 export 경로에서 전역 `document` 직접 참조를 제거하고, 의존성 주입 기반 `document/window` 해석 경로로 통일했습니다.
- persistence 조립 계층에서 `data-transfer`로 `document/window`를 전달하도록 연결해 런타임/테스트 환경 일관성을 강화했습니다.
- 관련 회귀 테스트를 추가해 DOM 의존성 주입 경로가 실제로 사용되는지 검증했습니다.

## [3.11.6] - 2026-03-26
### 변경됨
- 저장소 무결성 강화: state persistence payload를 envelope(`revision`, `updatedAt`, `data`) 형식으로 확장하고, `chrome.storage.local`/`localStorage` 후보를 비교해 최신 스냅샷을 우선 복원하도록 개선했습니다.
- 렌더링 안정성 강화: 테이블 재렌더링 전에 DatePicker 인스턴스를 정리(`destroy`)하도록 변경해 장시간 사용 시 누수 가능성을 줄였습니다.
- 테스트 훅 안정성 강화: `main.js` 테스트 훅 내부 `eval` 접근을 제거하고 화이트리스트 레지스트리 기반 접근으로 교체했습니다.
- 빌드 성능/품질 강화: `build_extension.ps1`에 번들 minify(`terser`)와 번들 크기 게이트(900KB)를 추가했습니다.

### 테스트
- 상태 영속화 회귀 테스트를 추가해 stale chrome 데이터가 최신 local 데이터를 덮어쓰지 않음을 검증했습니다.
- `npm run lint`, `npm run test:coverage`(108 files / 601 tests), `npm run build:strict` 통과를 확인했습니다.

## [3.11.5] - 2026-03-22
### 변경됨
- `main.js`에 집중되어 있던 전역 상태 변수(`globalTimes` 등)와 서비스 의존성 조립기(`GTV_MAIN_MODULE_RESOLVER`) 로직을 각각 `main-app-state-vars.js`, `main-core-service-assembly.js`로 분리해 추출했습니다.
- 이를 통해 `main.js`의 파일 크기를 800줄 가량 추가 절감하고 결합도를 획기적으로 낮췄습니다.
- 릴리스 버전 상향 (v3.11.5)

### 테스트
- 분리된 상태 모듈에 대한 단위 테스트(`main-app-state-vars-module.test.mjs`, `main-core-service-assembly-module.test.mjs`)를 추가/갱신했습니다.
- `npm run ci:gate`를 통해 416개의 전체 유닛 테스트 및 무결점 빌드를 확인했습니다.

## [3.11.4] - 2026-03-21
### 변경됨
- `main.js`의 주요 래퍼 함수를 도메인 파사드로 분리했습니다.
  - 시간 조정: `main-time-adjust-facade`
  - 시간대/테이블: `main-timezone-table-facade`
  - 고정 시간 탭: `main-fixed-time-tab-facade`
  - 연속 시간 변경 탭: `main-multi-range-tab-facade`
- 앱 초기화 흐름을 `main-app-bootstrap` 모듈로 분리해 진입점 책임을 축소했습니다.
- 모듈 스펙/로더/엔트리 스크립트 체인을 확장해 신규 파사드 모듈을 정식 등록했습니다.

### 테스트
- 도메인 파사드 모듈 단위 테스트를 추가했습니다.
- 메인 글로벌 API 스모크 테스트를 추가했습니다.
- `npm run ci:gate` 기준으로 lint, coverage, strict build까지 통과를 확인했습니다.

## [3.11.3] - 2026-03-20
### 변경됨
- 계산기 날짜 이동 로직을 UTC 기준 파싱 및 UTC day/week/month/year 연산으로 전환해 시간대 드리프트를 방지했습니다.
- 월/연 이동 시 오버플로우를 말일로 보정하도록 변경했습니다 (예: `2026-01-31 +1 month -> 2026-02-28`, `2024-02-29 +1 year -> 2025-02-28`).
- DatePicker 월 이동 시 `currentDate`를 1일로 고정한 뒤 월을 이동하도록 변경해 31일 기준 월 건너뛰기 문제를 해결했습니다.

### 테스트
- 계산기 회귀 테스트(월 오버플로우, 윤년 오버플로우, DST 경계 자정 안정성)를 추가했습니다.
- DatePicker 회귀 테스트(31일 기준 이전/다음 달 이동)를 추가했습니다.
- 커버리지 임계치 포함 `npm run ci:gate` 통과를 확인했습니다.

## [3.11.2] - 2026-03-19
### 변경됨
- 메인 부트스트랩에서 포맷 프로필 초기화 시점을 서비스 배선 완료 이후로 이동해 초기화 계약을 고정했습니다.
- 전역 상태 조회를 selector/accessor 경유로 정리해 `main.js` 오케스트레이션 결합도를 낮췄습니다.
- 패치 상태 포맷 설정 조회 경로를 통일해 포맷 관련 상태 읽기 일관성을 강화했습니다.

### 테스트
- 주요 회귀 테스트 및 `npm run ci:gate` 통과를 기준으로 검증했습니다.

## [3.11.1] - 2026-03-18
### 추가됨
- 시간대 약자 배지(`.zone-code`)에 타입별 시각 구분 추가
  - 표준 시간대: 기본 스타일 유지
  - 커스텀 시간대: 별도 배경/테두리/텍스트 색상 적용
- 커스텀 시간대 중복 등록 방지
  - 같은 그룹 내 동일 약어(정규화 기준)가 이미 있으면 토스트 표시 후 추가 차단
- 표준 시간대 중복 등록 방지
  - 같은 그룹 내 동일 IANA 시간대(`zone`)가 이미 있으면 토스트 표시 후 추가 차단

### 변경됨
- DST 라벨 표시 범위를 표 UI 중심으로 정리
  - 테이블 UI(실시간/고정/연속 시간 변경)의 지역명에만 `[DST]` 표시
  - 복사/내보내기 텍스트에는 `[DST]` 미포함 유지
- DST 지역 시간대 검색/추가 흐름 정리
  - 목록에서 동일 시간대가 중복으로 보이지 않도록 `country` 리스트는 1개 항목만 노출
  - DST 지역 항목 추가 시 고정 오프셋이 아닌 동적 시간대로 저장되도록 조정

### 테스트
- 중복 추가 방지 유닛 테스트 보강
  - 커스텀 약어 중복 차단
  - 표준 시간대(`zone`) 중복 차단
- DST UI/검색 경로 관련 테스트 업데이트 및 회귀 검증

## [3.10.3] - 2026-03-18
### 변경됨
- 릴리스 버전 상향 (v3.10.3)

## [3.10.2] - 2026-03-16
### 수정됨
- 사용자 지정 시간대(Custom Timezone)에서 올바른 UTC 오프셋이 반영되지 않고 기본(0) 오프셋으로 처리되던 현상 수정

### 변경됨
- 고정 시간(Fixed Time) 탭의 시간대 테이블에서 순서 조정(드래그 앤 드롭 행 이동) 기능 제거
- 릴리스 버전 상향 (v3.10.2)

## [3.10.1] - 2026-03-15
### 변경됨
- main.js 책임 분리 리팩토링 확장
  - 시간 조정 액션 코어 분리: time-adjust-actions.js
  - 앱 피드백(토스트/치명 오류 배너) 분리: app-feedback.js
  - 이미지 내보내기 브리지 분리: image-export-bridge.js
  - 계산기 액션(initCalculators, copyText) 분리: calculator-actions.js
- 고정 시간 테이블 슬롯 헤더 구조/스타일 개선
  - 슬롯 타이틀이 셀 기준 중앙 정렬되도록 헤더 레이아웃 재구성
  - 슬롯 타이틀 글자 크기를 테이블 헤더 가독성에 맞게 상향
- 릴리스 버전 상향 (v3.10.1)

### 수정됨
- 시간 변경/연속 시간 변경의 시간 조정이 기준 시간대가 아닌 UTC 기준으로 적용되던 문제 수정
  - fixedOffsetMinutes: null 값이 0으로 강제 변환되던 로직 보정
  - 기준 시간대(base timezone) 기준 조정이 정상 동작하도록 수정

## [3.9.3] - 2026-03-14
### 변경됨
- 계산기 도구의 `Unix 타임스탬프 변환기`를 `Smart Format Converter`로 개편
  - `Unix Timestamp (s/ms)` / `ISO 8601 (Local/UTC)` / `RFC 2822` / `SQL` / `Human Readable` 포맷 지원
  - 입력 필드 중 하나를 수정하면 나머지 포맷이 실시간으로 동기화되도록 개선
  - `Now` 동기화 버튼과 포맷별 복사 버튼 추가
- 좌측 네비게이션 순서를 `실시간 > 시간 변경 > 연속 시간 변경 > 고정 시간 > 계산기 도구`로 정리
- `고정 시간`과 `계산기 도구` 메뉴 사이 구분선 추가
- 릴리스 버전 상향 (v3.9.3)

### 수정됨
- `고정 시간` 탭에서 누락된 슬롯 수량 조절 UI(`fixed-time-slot-count-*`) 복구
- 계산기 모듈 테스트를 Smart Format Converter UI/동작 기준으로 갱신

## [3.9.2] - 2026-03-14
### 수정됨
- 고정 시간 타임라인 인디게이터 드래그 안정성 개선
  - 포인터 이벤트 처리 보강(드래그 중 유실/끊김 완화)
  - 드래그 중 불필요한 렌더/저장 최소화로 버벅임 완화
- 고정 시간 타임라인 인디게이터 라인 히트 영역 확장
  - 라벨뿐 아니라 라인 자체 드래그도 안정적으로 동작하도록 개선
- 연속 시간 변경 탭에서 `표기 / 복사 양식 > 시간 > 항목`의 `날짜/시간` 체크 상태가 시간대 테이블에 즉시 반영되도록 수정
- 고정 시간 탭에서 `표기 / 복사 양식 > 시간 > 항목 > 시간` 체크 해제 시 슬롯 시간 입력/버튼 표시가 반영되도록 수정

### 변경됨
- 타임라인 인디게이터 스타일/동작을 실시간·시간 변경 탭에도 일관 적용
  - 인디게이터 라인 히트 영역 확대
  - 커서(`<->`)가 인디게이터 조절 가능 영역에서만 표시되도록 조정
- 시간 변경 탭에서 타임라인 인디게이터 라벨 표시 추가
  - 추가 시간 = `false`: `시간`
  - 추가 시간 = `true`: `시작 시간`, `끝 시간`
- 시간 변경 탭에서 추가 시간 표시 시 패널 상단의 `시작 시간/끝 시간` 타이틀 제거(인디게이터 라벨로 대체)
- 고정 시간 슬롯 시간 입력 폭을 `100px`로 조정
- 릴리스 버전 상향 (v3.9.2)

## [3.9.1] - 2026-03-11
### 추가됨
- **최종 코드 감사 이슈 해결**:
  - `main.js` 내 인코딩 손상 한글 문자열(기본 그룹 등) 완벽 복구
  - 시간 계산 로직(`resolveLocalDatePartsByTimezoneAtDate`)의 DOM 의존 폴백 제거 및 안정성 강화
  - 모든 UI 이벤트 핸들러를 `addEventListener` 방식으로 교체하여 유지보수성 향상

### 변경됨
- 릴리스 버전 상향 (v3.9.1)

## [3.9.0] - 2026-03-10
### 추가됨
- **Luxon 마이그레이션 완료**: `main.js` 내의 모든 `Intl.DateTimeFormat` 직접 사용을 제거하고 Luxon 엔진 기반 `GTVTimeService`로 완전 전환
- **계산기 도구 개선**:
  - 시작일이 종료일보다 나중일 경우 음수(-) 기간 정상 표시 (Math.trunc 적용)
  - 시작일/종료일 교체(Swap) 기능 시 날짜 선택기(CDP) 동기화 오류 수정
  - 날짜 계산 시 UTC 자정 정규화를 통해 DST(일광절약시간) 경계 오류 원천 차단
  - 계산 결과에 i18n 단위 접미사(일, 시간, 분, 초) 추가
### 변경됨
- 코드 구조 최적화: `main.js` 내 약 70여 라인의 중복 함수 정의 제거 및 모듈화 강화
- 릴리스 버전 상향 (v3.9.0)

## [3.8.1] - 2026-03-08
### 수정됨
- 시간 변경 탭에서 시간이 자동으로 갱신되는 버그 수정 (`isRealtime` 전역 동기화 보정)

### 변경됨
- 연속 시간 변경 탭의 테이블 컬럼 너비 확장 (UTC 오프셋, 기간 등 가독성 개선)
- 릴리스 버전 상향 (v3.8.1)

## [3.8.0] - 2026-03-08
### 변경됨
- 시간 편집 테이블(시간 변경/연속 시간 변경) 내 달력 아이콘 위치 조정 (요일 뒤로 이동)
- 릴리스 버전 상향 (v3.8.0)

## [3.7.3] - 2026-03-08
### 변경됨
- Unix 타임스탬프 변환기 입력/출력 전 구간 UTC 표준 동기화 (`Now` 클릭 시 입력창에도 `Z` 표시)
- 릴리스 버전 상향 (v3.7.3)

## [3.7.2] - 2026-03-08
### 변경됨
- Unix 타임스탬프 변환기 출력 형식 개선 (`YYYY-MM-DDTHH:mm:ssZ` 표준 적용)
- 타임스탬프 변환 시 로컬 시간이 아닌 실제 UTC 시간으로 출력되도록 수정
- 릴리스 버전 상향 (v3.7.2)

## [3.7.1] - 2026-03-08
### 추가됨
- 데이터 가져오기(Import) 유효성 검증 로직 강화 (JSON 파싱 오류 및 스키마 불일치 감지)
- 가져오기 실패 시 "잘못된 데이터 형식입니다" 토스트 피드백 제공
- 데이터 원자적 처리 보장 (검증 실패 시 기존 데이터 보존)

### 변경됨
- 릴리스 버전 상향 (v3.7.1)

## [3.7.0] - 2026-03-08
### 변경됨
- DOM 조작 관련 보안 취약점 사전 차단 (`innerHTML` 사용을 `textContent` 및 안전한 API로 전환)
- `localStorage` 동기 호출 기반을 브라우저 확장 프로그램 권장 사항인 `chrome.storage.local` 비동기 패턴으로 전면 마이그레이션
- Vite 번들러(Minify/Uglify)를 활용한 확장 프로그램 최적화 빌드 파이프라인 도입
- 릴리스 버전 상향 (v3.7.0)

## [3.6.0] - 2026-03-08
### 변경됨
- 내보내기 기능 개편: '이미지 저장 - 타임라인' 제거 및 '이미지 저장 - 구간' 기능 복구
- 시간대 표 이미지 저장 시 달력 아이콘 제외 처리
- '복사 - 구간' 버튼에 아이콘 추가 및 명칭 변경
- 타임라인 시간대 이름 툴팁 제거 (가독성 개선)
- 릴리스 버전 상향 (v3.6.0)

## [3.5.3] - 2026-03-08
### 추가됨
- 복사 및 이미지 내보내기 피드백을 위한 새로운 토스트 알림 시스템
- 커스텀 고스트 이미지 및 레이아웃 전환 애니메이션이 포함된 강화된 드래그 앤 드롭
- 중앙 집중식 버전 및 상수 관리를 위한 `app-config.js` 추가

## [3.5.2] - 2026-03-07
### 변경됨
- 런타임 및 패키지 표면 전반의 버전 메타데이터(`3.5.2`) 동기화
- 안전한 스타트업 렌더링을 위해 `index.html` 내의 정적 폴백 텍스트 정리
- 레거시/데드 퍼시스턴스 필드 `showTimeAdjust` 제거
- 레거시 스토리지 폴백 컷오프 정책 추가 (자동 마이그레이션 로드의 경우 `v320+`만 해당)
- `README.md` 재작성 및 변경 이력 인코딩 콘텐츠 정규화
- 안정성/무결성 검토 문서 추가

## [3.5.1] - 2026-03-07
### 변경됨
- 릴리스 버전/캐시 쿼리 업데이트 (v3.5.1, cache v=351)
- 실시간 및 시간 편집 탭에 드래그 가능한 시작/종료 가이드 라인이 있는 타임라인 뷰 추가
- 타임라인 드래그 시 실시간으로 시간대 테이블 시간 업데이트
- 시간 편집에서 실시간으로 전환할 때 지연되는 실시간 동기화 수정
- 타임라인 UX 개선: 즉각적인 행 정렬 반영, 툴팁 정리 및 스타일 일관성

## [3.4.4] - 2026-03-07
### 변경됨
- 릴리스 버전/캐시 쿼리 업데이트 (v3.4.4, cache v=344)
- 국가/지역 시간대 팝업 목록에 UTC 오프셋 배지 추가 (예: `[KST] [UTC +09:00]`)
- 동적인 시간대 오프셋이 고정된 `UTC+00:00`으로 처리되던 회귀 버그 수정
- UI 폴리싱: 계산기 복사 툴팁/스타일 통합 및 드래그 핸들 정렬 조정
- 기본 그룹 이름을 `그룹`으로 변경
- 낮 아이콘을 `☀️`로 변경

## [3.4.3] - 2026-03-05
### 변경됨
- 릴리스 버전/캐시 쿼리 업데이트 (v3.4.3, cache v=343)

## [3.4.1] - 2026-03-05
### 변경됨
- 릴리스 버전/캐시 쿼리 업데이트 (v3.4.1, cache v=341)
