# 변경 이력

## [3.12.7] - 2026-04-13
### 변경됨
- 버전 메타데이터를 `3.12.7`로 상향했습니다. (`package.json`, `package-lock.json`, `manifest.json`, `js/modules/app-config.js`, `README.md`)
- `표기 / 복사 양식`에서 드래그로 변경한 순서가 시간대 테이블 표기와 텍스트 복사 결과에 즉시 반영되도록 수정했습니다.
- `표기 / 복사 양식` 설정(순서/활성 상태)이 저장되지 않아 새로고침 후 초기화되던 문제를 수정했습니다.
- `복사 양식 > 시간 > 항목` 드롭다운이 다른 프레임 아래로 가려지던 레이어(z-index) 문제를 수정했습니다.
- `표기 / 복사 양식` 플래그를 탭 공통 상태로 정리해, 탭 이동 시 토글 체크 상태와 실제 UI 표시가 불일치하던 문제를 수정했습니다.

## [3.12.6] - 2026-04-11
### 변경됨
- 버전 메타데이터를 `3.12.6`으로 상향했습니다. (`package.json`, `package-lock.json`, `manifest.json`, `js/modules/app-config.js`, `README.md`)
- 시간대 테이블의 기준 시간 행에서 순서 셀에 `기준` 라벨이 표시되도록 개선했습니다.

## [3.12.5] - 2026-04-11
### 변경됨
- 버전 메타데이터를 `3.12.5`로 상향했습니다. (`package.json`, `package-lock.json`, `manifest.json`, `js/modules/app-config.js`, `README.md`)
- 배포본(`dist_extension`)에서 일부 시간대 오프셋이 `UTC+00:00`으로 계산되던 문제를 수정했습니다.
- 패키징 빌드에서 전역 `luxon` 해석 안정화를 위해 `js/vendor/luxon-global-bridge.js`를 추가하고 로더 순서를 갱신했습니다.

## [3.12.4] - 2026-04-05
### 변경됨
- 버전 메타데이터를 `3.12.4`로 상향했습니다. (`package.json`, `package-lock.json`, `manifest.json`, `js/modules/app-config.js`, `README.md`, 테스트 기대값)
- 통합 데이터 저장 키를 `GTV_v324_Data`로 상향하고, 이전 키(`GTV_v323_Data`)를 레거시 마이그레이션 키로 추가했습니다.
- 아키텍처 문서를 `README.md`로 통합하고 현재 런타임 구조에 맞춰 내용을 최신화했습니다.
- 모듈 의존성 호출 스타일을 `toSafeCallable(depName, depFn)` 패턴으로 통일하고 의존성 예외 로깅 형식을 정리했습니다.
- 코드 스타일 회귀 방지를 위해 모듈 규약 테스트에 `legacy toSafeCallable/invokeDep` 금지 검증을 추가했습니다. (`tests/module-create-service-convention.test.mjs`)

### 삭제됨
- `docs/ARCHITECTURE.md` (README로 통합 완료)

## [3.12.3] - 2026-04-05
### 변경됨
- 버전 메타데이터를 `3.12.3`으로 상향했습니다. (`package.json`, `package-lock.json`, `manifest.json`, `js/modules/app-config.js`, `README.md`)
- 고정 시간 탭 타임라인에서 `실시간 표시` 인디케이터가 누락되던 wiring 문제를 수정해 토글 상태가 즉시 반영되도록 개선했습니다.
- 고정 시간 탭 타임라인의 `실시간 표시` 인디케이터 색상을 `#00E676`으로 조정해 가시성을 높였습니다.
- 고정 시간 탭 시간대 테이블의 `실시간` 헤더에도 고정 슬롯과 동일한 원형 색상 아이콘을 추가했습니다.
- 고정 시간 탭 타임라인의 `실시간 표시` 인디케이터는 드래그 대상이 아니므로 위/아래 삼각형 핸들을 제거했습니다.
- 빌드 엄격 모드(`build:strict`)의 번들 크기 게이트를 현재 산출물 기준으로 현실화했습니다. (`900KB -> 950KB`)

## [3.12.1] - 2026-03-31
### 변경됨
- 고정 시간 탭의 실시간 표시 기능 사용 시 발생하던 UI 깜빡임과 리프레시 현상을 수정했습니다.
- 고정 시간 탭의 모든 시간 입력창 너비를 85px로 조정하고 텍스트 중앙 정렬을 적용했습니다.
- 실시간 표시(Live Now) 항목의 폰트 굵기를 일반(`normal`)으로 조정해 시각적 일관성을 높였습니다.
- 타임라인에 고정 시간 모드 전용 `실시간 표시` 인디케이터가 움직이도록 기능을 추가했습니다.
- 내부 브리지 및 파사드 계층에서 매개변수 `isTick` 전달이 누락되던 문제를 해결했습니다.
- 버전 `3.12.1`에 맞춰 모든 단위 테스트(863개)와 기대값(Version, Width)을 갱신했습니다.

## [3.11.9] - 2026-03-29
### 변경됨
- 버전 메타데이터를 `3.11.9`로 상향했습니다. (`package.json`, `package-lock.json`, `manifest.json`, `js/modules/app-config.js`, `README.md`)
- 버전 노출 테스트 기대값을 `3.11.9`로 갱신했습니다. (`tests/main-runtime-host-utils-module.test.mjs`)

## [3.11.8] - 2026-03-28
### 변경됨
- 버전 메타데이터를 `3.11.8`로 상향했습니다. (`package.json`, `package-lock.json`, `manifest.json`, `js/modules/app-config.js`, `README.md`)

## [3.11.7] - 2026-03-27
### 변경됨
- 버전 메타데이터를 `3.11.7`로 상향했습니다. (`package.json`, `package-lock.json`, `manifest.json`, `js/modules/app-config.js`)
- `data-transfer.js`의 그룹/보조 그룹/전체 설정 export 경로에서 전역 `document` 직접 참조를 제거하고, 의존성 주입 기반 `document/window` 해석 경로로 통일했습니다.
- persistence 조립 계층에서 `data-transfer`로 `document/window`를 전달하도록 연결해 런타임과 테스트 환경의 일관성을 강화했습니다.
- 관련 회귀 테스트를 추가해 DOM 의존성 주입 경로가 실제로 사용되는지 검증했습니다.

## [3.11.6] - 2026-03-26
### 변경됨
- 저장소 무결성을 강화했습니다. state persistence payload를 envelope(`revision`, `updatedAt`, `data`) 형식으로 확장하고, `chrome.storage.local`과 `localStorage` 후보를 비교해 최신 스냅샷을 우선 복원하도록 개선했습니다.
- 렌더링 안정성을 강화했습니다. 테이블 재렌더링 전에 DatePicker 인스턴스를 정리(`destroy`)하도록 변경해 장시간 사용 시 누수 가능성을 줄였습니다.
- 테스트 훅 안정성을 강화했습니다. `main.js` 테스트 훅 내부의 `eval` 접근을 제거하고 화이트리스트 레지스트리 기반 접근으로 교체했습니다.
- 빌드 성능과 품질을 강화했습니다. `build_extension.ps1`에 번들 minify(`terser`)와 번들 크기 게이트(900KB)를 추가했습니다.

### 테스트
- 상태 영속화 회귀 테스트를 추가해 stale chrome 데이터가 최신 local 데이터를 덮어쓰지 않음을 검증했습니다.
- `npm run lint`, `npm run test:coverage`(108 files / 601 tests), `npm run build:strict` 통과를 확인했습니다.

## [3.11.5] - 2026-03-22
### 변경됨
- `main.js`에 집중되어 있던 전역 상태 변수(`globalTimes` 등)와 서비스 의존성 조립기(`GTV_MAIN_MODULE_RESOLVER`) 로직을 각각 `main-app-state-vars.js`, `main-core-service-assembly.js`로 분리해 추출했습니다.
- 이를 통해 `main.js`의 파일 크기를 약 800줄 줄이고 결합도를 크게 낮췄습니다.
- 릴리스 버전을 상향했습니다. (`v3.11.5`)

### 테스트
- 분리된 상태 모듈에 대한 단위 테스트(`main-app-state-vars-module.test.mjs`, `main-core-service-assembly-module.test.mjs`)를 추가하거나 갱신했습니다.
- `npm run ci:gate`를 통해 전체 유닛 테스트 416개와 무결점 빌드를 확인했습니다.

## [3.11.4] - 2026-03-21
### 변경됨
- `main.js`의 주요 래퍼 함수를 도메인 파사드로 분리했습니다.
  - 시간 조정: `main-time-adjust-facade`
  - 시간대/테이블: `main-timezone-table-facade`
  - 고정 시간 탭: `main-fixed-time-tab-facade`
  - 연속 시간 변경 탭: `main-multi-range-tab-facade`
- 앱 초기화 흐름을 `main-app-bootstrap` 모듈로 분리해 진입점 책임을 축소했습니다.
- 모듈 스펙, 로더, 엔트리 스크립트 체인을 확장해 신규 파사드 모듈을 정식 등록했습니다.

### 테스트
- 도메인 파사드 모듈 단위 테스트를 추가했습니다.
- 메인 글로벌 API 스모크 테스트를 추가했습니다.
- `npm run ci:gate` 기준으로 lint, coverage, strict build까지 통과를 확인했습니다.

## [3.11.3] - 2026-03-20
### 변경됨
- 계산기 날짜 이동 로직을 UTC 기준 파싱 및 UTC day/week/month/year 연산으로 전환해 시간대 드리프트를 방지했습니다.
- 월/연 이동 시 오버플로우를 말일로 보정하도록 변경했습니다. 예: `2026-01-31 +1 month -> 2026-02-28`, `2024-02-29 +1 year -> 2025-02-28`
- DatePicker 월 이동 시 `currentDate`를 1일로 고정한 뒤 월을 이동하도록 변경해 31일 기준 월 건너뛰기 문제를 해결했습니다.

### 테스트
- 계산기 회귀 테스트(월 오버플로우, 윤년 오버플로우, DST 경계 자정 안정성)를 추가했습니다.
- DatePicker 회귀 테스트(31일 기준 이전/다음 달 이동)를 추가했습니다.
- 커버리지 임계치 포함 `npm run ci:gate` 통과를 확인했습니다.

## [3.11.2] - 2026-03-19
### 변경됨
- 메인 부트스트랩에서 포맷 프로필 초기화 시점을 서비스 배선 완료 이후로 이동해 초기화 계약을 고정했습니다.
- 전역 상태 조회를 selector/accessor 경유로 정리해 `main.js` 오케스트레이션 결합도를 낮췄습니다.
- 패치 상태 포맷 설정 조회 경로를 통일해 포맷 관련 상태 읽기 일관성을 강화했습니다.

### 테스트
- 주요 회귀 테스트와 `npm run ci:gate` 통과를 기준으로 검증했습니다.

## [3.11.1] - 2026-03-18
### 추가됨
- 시간대 약자 배지(`.zone-code`)에 타입별 시각 구분을 추가했습니다.
  - 표준 시간대: 기본 스타일 유지
  - 커스텀 시간대: 별도 배경, 테두리, 텍스트 색상 적용
- 커스텀 시간대 중복 등록 방지를 추가했습니다.
  - 같은 그룹 내 동일 약어(정규화 기준)가 이미 있으면 토스트를 표시하고 추가를 차단합니다.
- 표준 시간대 중복 등록 방지를 추가했습니다.
  - 같은 그룹 내 동일 IANA 시간대(`zone`)가 이미 있으면 토스트를 표시하고 추가를 차단합니다.

### 변경됨
- DST 라벨 표시 범위를 표 UI 중심으로 정리했습니다.
  - 테이블 UI(실시간, 고정 시간, 연속 시간 변경)의 지역명에만 `[DST]`를 표시합니다.
  - 복사 및 내보내기 텍스트에는 `[DST]`를 포함하지 않습니다.
- DST 지역 시간대 검색/추가 흐름을 정리했습니다.
  - 목록에서 동일 시간대가 중복으로 보이지 않도록 `country` 리스트는 1개 항목만 노출합니다.
  - DST 지역 항목 추가 시 고정 오프셋이 아닌 동적 시간대로 저장되도록 조정했습니다.

### 테스트
- 중복 추가 방지 유닛 테스트를 보강했습니다.
  - 커스텀 약어 중복 차단
  - 표준 시간대(`zone`) 중복 차단
- DST UI 및 검색 경로 관련 테스트를 업데이트하고 회귀를 검증했습니다.

## [3.10.3] - 2026-03-18
### 변경됨
- 릴리스 버전을 상향했습니다. (`v3.10.3`)

## [3.10.2] - 2026-03-16
### 수정됨
- 사용자 지정 시간대(Custom Timezone)에서 올바른 UTC 오프셋이 반영되지 않고 기본값 `0` 오프셋으로 처리되던 현상을 수정했습니다.

### 변경됨
- 고정 시간(Fixed Time) 탭의 시간대 테이블에서 순서 조정 기능(드래그 앤 드롭 행 이동)을 제거했습니다.
- 릴리스 버전을 상향했습니다. (`v3.10.2`)

## [3.10.1] - 2026-03-15
### 변경됨
- `main.js` 책임 분리 리팩토링을 확장했습니다.
  - 시간 조정 액션 코어 분리: `time-adjust-actions.js`
  - 앱 피드백(토스트/치명 오류 배너) 분리: `app-feedback.js`
  - 이미지 내보내기 브리지 분리: `image-export-bridge.js`
  - 계산기 액션(`initCalculators`, `copyText`) 분리: `calculator-actions.js`
- 고정 시간 테이블 슬롯 헤더 구조와 스타일을 개선했습니다.
  - 슬롯 타이틀이 셀 기준 중앙 정렬되도록 헤더 레이아웃을 재구성했습니다.
  - 슬롯 타이틀 글자 크기를 테이블 헤더 가독성에 맞게 상향했습니다.
- 릴리스 버전을 상향했습니다. (`v3.10.1`)

### 수정됨
- 시간 변경과 연속 시간 변경의 시간 조정이 기준 시간대가 아닌 UTC 기준으로 적용되던 문제를 수정했습니다.
  - `fixedOffsetMinutes: null` 값이 `0`으로 강제 변환되던 로직을 보정했습니다.
  - 기준 시간대(base timezone) 기준 조정이 정상 동작하도록 수정했습니다.

## [3.9.3] - 2026-03-14
### 변경됨
- 계산기 도구의 `Unix 타임스탬프 변환기`를 `Smart Format Converter`로 개편했습니다.
  - `Unix Timestamp (s/ms)`, `ISO 8601 (Local/UTC)`, `RFC 2822`, `SQL`, `Human Readable` 포맷을 지원합니다.
  - 입력 필드 중 하나를 수정하면 나머지 포맷이 실시간으로 동기화되도록 개선했습니다.
  - `Now` 동기화 버튼과 포맷별 복사 버튼을 추가했습니다.
- 좌측 네비게이션 순서를 `실시간 > 시간 변경 > 연속 시간 변경 > 고정 시간 > 계산기 도구`로 정리했습니다.
- `고정 시간`과 `계산기 도구` 메뉴 사이에 구분선을 추가했습니다.
- 릴리스 버전을 상향했습니다. (`v3.9.3`)

### 수정됨
- `고정 시간` 탭에서 누락된 슬롯 수량 조절 UI(`fixed-time-slot-count-*`)를 복구했습니다.
- 계산기 모듈 테스트를 Smart Format Converter UI와 동작 기준으로 갱신했습니다.

## [3.9.2] - 2026-03-14
### 수정됨
- 고정 시간 타임라인 인디케이터 드래그 안정성을 개선했습니다.
  - 포인터 이벤트 처리를 보강해 드래그 중 유실과 끊김을 완화했습니다.
  - 드래그 중 불필요한 렌더와 저장을 최소화해 버벅임을 줄였습니다.
- 고정 시간 타임라인 인디케이터 라인의 히트 영역을 확장했습니다.
  - 라벨뿐 아니라 라인 자체도 안정적으로 드래그할 수 있도록 개선했습니다.
- 연속 시간 변경 탭에서 `표기 / 복사 양식 > 시간 > 항목`의 `날짜/시간` 체크 상태가 시간대 테이블에 즉시 반영되도록 수정했습니다.
- 고정 시간 탭에서 `표기 / 복사 양식 > 시간 > 항목 > 시간` 체크 해제 시 슬롯 시간 입력과 버튼 표시가 즉시 반영되도록 수정했습니다.

### 변경됨
- 타임라인 인디케이터 스타일과 동작을 실시간 탭과 시간 변경 탭에도 일관되게 적용했습니다.
  - 인디케이터 라인 히트 영역을 확대했습니다.
  - 커서(`<->`)가 인디케이터 조절 가능 영역에서만 표시되도록 조정했습니다.
- 시간 변경 탭에서 타임라인 인디케이터 라벨 표시를 추가했습니다.
  - 추가 시간 = `false`: `시간`
  - 추가 시간 = `true`: `시작 시간`, `끝 시간`
- 시간 변경 탭에서 추가 시간 표시 시 패널 상단의 `시작 시간/끝 시간` 타이틀을 제거하고 인디케이터 라벨로 대체했습니다.
- 고정 시간 슬롯 시간 입력 폭을 `100px`로 조정했습니다.
- 릴리스 버전을 상향했습니다. (`v3.9.2`)

## [3.9.1] - 2026-03-11
### 추가됨
- **최종 코드 감사 이슈 해결**
  - `main.js` 내부의 인코딩 손상 한글 문자열(기본 그룹 등)을 완전히 복구했습니다.
  - 시간 계산 로직 `resolveLocalDatePartsByTimezoneAtDate`의 DOM 의존 폴백을 제거하고 안정성을 강화했습니다.
  - 모든 UI 이벤트 핸들러를 `addEventListener` 방식으로 교체해 유지보수성을 높였습니다.

### 변경됨
- 릴리스 버전을 상향했습니다. (`v3.9.1`)

## [3.9.0] - 2026-03-10
### 추가됨
- **Luxon 마이그레이션 완료**: `main.js` 내의 모든 `Intl.DateTimeFormat` 직접 사용을 제거하고 Luxon 엔진 기반 `GTVTimeService`로 완전히 전환했습니다.
- **계산기 도구 개선**
  - 시작일이 종료일보다 나중일 경우 음수(-) 기간을 정상 표시하도록 `Math.trunc`를 적용했습니다.
  - 시작일/종료일 교체(Swap) 기능 사용 시 날짜 선택기(CDP) 동기화 오류를 수정했습니다.
  - 날짜 계산 시 UTC 자정 정규화를 통해 DST(일광절약시간) 경계 오류를 원천 차단했습니다.
  - 계산 결과에 i18n 단위 접미사(일, 시간, 분, 초)를 추가했습니다.

### 변경됨
- 코드 구조를 최적화했습니다. `main.js` 내 약 70여 줄의 중복 함수 정의를 제거하고 모듈화를 강화했습니다.
- 릴리스 버전을 상향했습니다. (`v3.9.0`)

## [3.8.1] - 2026-03-08
### 수정됨
- 시간 변경 탭에서 시간이 자동으로 갱신되던 버그를 수정했습니다. (`isRealtime` 전역 동기화 보정)

### 변경됨
- 연속 시간 변경 탭의 테이블 컬럼 너비를 확장해 UTC 오프셋과 기간 등의 가독성을 개선했습니다.
- 릴리스 버전을 상향했습니다. (`v3.8.1`)

## [3.8.0] - 2026-03-08
### 변경됨
- 시간 편집 테이블(시간 변경/연속 시간 변경) 내부 달력 아이콘 위치를 조정했습니다. (요일 뒤로 이동)
- 릴리스 버전을 상향했습니다. (`v3.8.0`)

## [3.7.3] - 2026-03-08
### 변경됨
- Unix 타임스탬프 변환기 입력과 출력 전 구간의 UTC 표준 동기화를 맞췄습니다. (`Now` 클릭 시 입력창에도 `Z` 표시)
- 릴리스 버전을 상향했습니다. (`v3.7.3`)

## [3.7.2] - 2026-03-08
### 변경됨
- Unix 타임스탬프 변환기 출력 형식을 개선했습니다. (`YYYY-MM-DDTHH:mm:ssZ` 표준 적용)
- 타임스탬프 변환 시 로컬 시간이 아닌 실제 UTC 시간으로 출력되도록 수정했습니다.
- 릴리스 버전을 상향했습니다. (`v3.7.2`)

## [3.7.1] - 2026-03-08
### 추가됨
- 데이터 가져오기(Import) 유효성 검증 로직을 강화했습니다. JSON 파싱 오류와 스키마 불일치를 감지합니다.
- 가져오기 실패 시 `잘못된 데이터 형식입니다` 토스트 피드백을 제공합니다.
- 검증 실패 시 기존 데이터를 보존하도록 데이터 원자성을 보장했습니다.

### 변경됨
- 릴리스 버전을 상향했습니다. (`v3.7.1`)

## [3.7.0] - 2026-03-08
### 변경됨
- DOM 조작 관련 보안 취약점을 사전에 차단했습니다. `innerHTML` 사용을 `textContent` 및 안전한 API로 전환했습니다.
- `localStorage` 동기 호출 기반을 브라우저 확장 프로그램 권장 사항인 `chrome.storage.local` 비동기 패턴으로 전면 마이그레이션했습니다.
- Vite 번들러를 활용한 확장 프로그램 최적화 빌드 파이프라인을 도입했습니다.
- 릴리스 버전을 상향했습니다. (`v3.7.0`)

## [3.6.0] - 2026-03-08
### 변경됨
- 내보내기 기능을 개편했습니다. `이미지 저장 - 타임라인`을 제거하고 `이미지 저장 - 구간` 기능을 복구했습니다.
- 시간대 표 이미지를 저장할 때 달력 아이콘을 제외하도록 처리했습니다.
- `복사 - 구간` 버튼에 아이콘을 추가하고 명칭을 변경했습니다.
- 타임라인 시간대 이름 툴팁을 제거해 가독성을 개선했습니다.
- 릴리스 버전을 상향했습니다. (`v3.6.0`)

## [3.5.3] - 2026-03-08
### 추가됨
- 복사 및 이미지 내보내기 피드백을 위한 새로운 토스트 알림 시스템을 추가했습니다.
- 커스텀 고스트 이미지와 레이아웃 전환 애니메이션이 포함된 강화된 드래그 앤 드롭을 추가했습니다.
- 중앙 집중식 버전 및 상수 관리를 위한 `app-config.js`를 추가했습니다.

## [3.5.2] - 2026-03-07
### 변경됨
- 런타임과 패키지 표면 전반의 버전 메타데이터(`3.5.2`)를 동기화했습니다.
- 안전한 스타트업 렌더링을 위해 `index.html` 내 정적 폴백 텍스트를 정리했습니다.
- 레거시/사용되지 않는 persistence 필드 `showTimeAdjust`를 제거했습니다.
- 레거시 스토리지 폴백 컷오프 정책을 추가했습니다. 자동 마이그레이션 로드는 `v320+`만 해당합니다.
- `README.md`를 재작성하고 변경 이력의 인코딩 콘텐츠를 정규화했습니다.
- 안정성과 무결성 검토 문서를 추가했습니다.

## [3.5.1] - 2026-03-07
### 변경됨
- 릴리스 버전과 캐시 쿼리를 업데이트했습니다. (`v3.5.1`, `cache v=351`)
- 실시간 및 시간 편집 탭에 드래그 가능한 시작/종료 가이드 라인이 포함된 타임라인 뷰를 추가했습니다.
- 타임라인 드래그 시 시간대 테이블 시간이 실시간으로 업데이트되도록 했습니다.
- 시간 편집에서 실시간으로 전환할 때 지연되던 실시간 동기화를 수정했습니다.
- 타임라인 UX를 개선했습니다. 즉각적인 행 정렬 반영, 툴팁 정리, 스타일 일관성을 반영했습니다.

## [3.4.4] - 2026-03-07
### 변경됨
- 릴리스 버전과 캐시 쿼리를 업데이트했습니다. (`v3.4.4`, `cache v=344`)
- 국가/지역 시간대 팝업 목록에 UTC 오프셋 배지를 추가했습니다. 예: `[KST] [UTC +09:00]`
- 동적인 시간대 오프셋이 고정된 `UTC+00:00`으로 처리되던 회귀 버그를 수정했습니다.
- UI 마감도를 높였습니다. 계산기 복사 툴팁과 스타일을 통합하고 드래그 핸들 정렬을 조정했습니다.
- 기본 그룹 이름을 `그룹`으로 변경했습니다.
- 낮 아이콘을 `☀️`로 변경했습니다.

## [3.4.3] - 2026-03-05
### 변경됨
- 릴리스 버전과 캐시 쿼리를 업데이트했습니다. (`v3.4.3`, `cache v=343`)

## [3.4.1] - 2026-03-05
### 변경됨
- 릴리스 버전과 캐시 쿼리를 업데이트했습니다. (`v3.4.1`, `cache v=341`)
